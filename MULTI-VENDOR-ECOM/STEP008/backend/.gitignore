# -----------------------------------------------------------------------------
# Node.js
# -----------------------------------------------------------------------------

# Dependencies
/node_modules
/jspm_packages

# Dependency lock files that can be regenerated (only ignore if you use pnpm/yarn/npm exclusively)
# To avoid conflicts, it's generally recommended to commit *one* lock file (e.g., package-lock.json).
# Uncomment to ignore:
# yarn.lock
# pnpm-lock.yaml
# package-lock.json

# Builds and Logs
/dist
/build
/coverage
/log
/logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.nyc_output

# Environment
# Local configuration files that should not be committed to Git
.env
.env.*
.DS_Store # macOS system files
/tmp

# Editor/IDE files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Runtime data
/pids
/run
/sessions